import { ElectionDetailSkeleton } from "@/components/skeletons/ElectionSkeletons";

/**
 * Displays a skeleton UI while the election detail page loads.
 */
export default function Loading() {
  return <ElectionDetailSkeleton />;
}
